{{polaris-data-table/cell
  total=true
  truncate=truncate
  height=heights.firstObject
  fixed=(eq index 0)
  text=(if
    (eq index 0)
    totalsRowHeading
    total
  )
  contentType=contentType
}}
