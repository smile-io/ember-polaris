{{polaris-data-table/cell
  total=true
  truncate=truncate
  height=(get-array-item heights 1)
  fixed=(eq index 0)
  text=(if
    (eq index 0)
    totalsRowHeading
    total
  )
  contentType=contentType
}}
