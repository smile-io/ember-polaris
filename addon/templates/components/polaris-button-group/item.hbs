<div
  class={{this.cssClasses}}
  data-test-button-group-item
  role="button"
  {{on "focus" this.handleFocus}}
  {{on "blur" this.handleBlur}}
  ...attributes
>
  {{#if (has-block)}}
    {{yield}}
  {{else}}
    {{@text}}
  {{/if}}
</div>