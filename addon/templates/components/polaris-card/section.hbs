{{#with (component "polaris-card/section-header") as |sectionHeader|}}
  {{#if title}}
    {{component sectionHeader title=title}}
  {{/if}}

  {{#if hasBlock}}
    {{yield (hash
      header=sectionHeader
    )}}
  {{else}}
    {{text}}
  {{/if}}
{{/with}}
