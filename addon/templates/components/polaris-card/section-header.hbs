{{#if title}}
  {{#if (is-component-definition title)}}
    {{render-content title}}
  {{else}}
    {{polaris-subheading text=title}}
  {{/if}}
{{/if}}

{{#if hasBlock}}
  {{yield}}
{{else}}
  {{text}}
{{/if}}
