<div
  data-test-label
  class="Polaris-Label {{if @hidden "Polaris-Label--hidden"}}"
  ...attributes
>
  <label id={{this.labelId}} htmlFor={{@id}} class="Polaris-Label__Text">
    {{#if (has-block)}}
      {{yield}}
    {{else}}
      {{render-content @text}}
    {{/if}}
  </label>
</div>
