<div class="Polaris-Card" data-test-callout-card ...attributes>
  <div class="Polaris-CalloutCard__Container">
    {{#if @onDismiss}}
      <div class="Polaris-CalloutCard__Dismiss">
        <PolarisButton
          data-test-callout-card-dismiss-button
          @plain={{true}}
          @icon="CancelSmallMinor"
          @accessibilityLabel="Dismiss card"
          @onClick={{@onDismiss}}
        />
      </div>
    {{/if}}

    <PolarisCard::Section>
      <div class="Polaris-CalloutCard">
        <div class="Polaris-CalloutCard__Content">
          <div class="Polaris-CalloutCard__Title">
            <PolarisHeading @text={{@title}} />
          </div>

          <PolarisTextContainer>
            {{#if hasBlock}}
              {{yield}}
            {{else}}
              {{@text}}
            {{/if}}
          </PolarisTextContainer>

          <div class="Polaris-CalloutCard__Buttons">
            {{#let
              (component "polaris-button"
                text=@primaryAction.text
                accessibilityLabel=@primaryAction.accessibilityLabel
                disabled=@primaryAction.disabled
                primary=@primaryAction.primary
                destructive=@primaryAction.destructive
                icon=@primaryAction.icon
                loading=@primaryAction.loading
                onClick=@primaryAction.onAction
              )
              as |PrimaryButton|
            }}
              {{#if @secondaryAction}}
                <PolarisButtonGroup data-test-callout-card-button-group >
                  <PrimaryButton data-test-callout-card-primary-button />

                  <PolarisButton
                    data-test-callout-card-secondary-button
                    @plain={{true}}
                    @text={{@secondaryAction.text}}
                    @accessibilityLabel={{@secondaryAction.accessibilityLabel}}
                    @disabled={{@secondaryAction.disabled}}
                    @icon={{@secondaryAction.icon}}
                    @loading={{@secondaryAction.loading}}
                    @onClick={{@secondaryAction.onAction}}
                  />
                </PolarisButtonGroup>
              {{else}}
                <PrimaryButton data-test-callout-card-primary-button />
              {{/if}}
            {{/let}}
          </div>
        </div>

        {{!-- TODO use <PolarisImage> once implemented  --}}
        <img
          alt=""
          class="Polaris-CalloutCard__Image {{if @onDismiss "Polaris-CalloutCard__DismissImage"}}"
          src={{@illustration}}
        >
      </div>
    </PolarisCard::Section>
  </div>
</div>
